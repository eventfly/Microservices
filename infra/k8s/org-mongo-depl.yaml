apiVersion: apps/v1
kind: Deployment
metadata:
  name: org-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: org-mongo
  template:
    metadata:
      labels:
        app: org-mongo
    spec:
      containers:
        - name: org-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: org-mongo-srv
spec:
  selector:
    app: org-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
